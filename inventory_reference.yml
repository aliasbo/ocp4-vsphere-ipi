# This is the inventory file used by ansible to apply the playbooks
# The commented objects are optional, the rest are required.
---
all:
  hosts:
    localhost:
  vars:
##### OpenShift version. The default value is 'stable' or dotted version '4.7.7' (latest)
#    openshift_version: 'stable'
##### Number of worker nodes
<<<<<<< HEAD
#    workers: 3
=======
#    workers: 2
>>>>>>> single-master
##### Spec for the worker nodes
#    worker_spec:
#      cpus: 2
#      memoryMB: 8196
#      diskSizeGB: 120
##### Single node master?
#    single_master: false
##### Spec for the master nodes
#    master_spec:
#      cpus: 4
#      memoryMB: 16384
#      diskSizeGB: 120
##### IP address block for pods
#    pod_sdn_cidr: '10.128.0.0/14'
##### IP address block for pods
#    service_sdn_cidr: '172.30.0.0/16'
##### Name of the cluster
    cluster_name: <ocp4>
##### Parent DNS domain for the cluster
    base_domain: <example.com>
##### IP assigned to the DNS record api.<cluster_name>.<base_domain>
    api_ip: <'192.168.1.8'>
##### IP assigned to the DNS record *.apps.<cluster_name>.<base_domain>
    apps_ip: <'192.168.1.9'>
##### IP address of the NTP server
    ntp_ip: <'192.168.1.1'>
##### IP address of the DNS server
    dns_ip: <'192.168.1.1'>
##### vCenter access details
    vcenter_hostname: <'192.168.22.10'>
    vcenter_username: <'administrator@vsphere.local'>
    vcenter_datacenter: <Datacenter>
    vcenter_cluster: <Cluster>
    vcenter_datastore: <Datastore>
    vcenter_network: <NetworkName>
##### Pull Secret file
#    pull_secret_name: '<file-name>'
##### IP address and port of the Proxy
#    proxy_http: '192.168.1.1:3128'
#    no_proxy_http: 'example.com'
#    machine_cidr: '10.0.0.0/16'
##### For Disconnected installation
#    local_registry_server: '<IP:Port>'
#    local_registry_cert_name: '<certificate_file>'
#    local_registry_repository: '<ocp4/openshift4>'
##> Paste below the imageContentSources obtained from the 'oc adm release mirror'
#    local_registry_sources: |
#      imageContentSources:
#      - mirrors:
#        - local.registry:5000/ocp4/openshift4
#        source: quay.io/openshift-release-dev/ocp-release
#      - mirrors:
#        - local.registry:5000/ocp4/openshift4
#        source: quay.io/openshift-release-dev/ocp-v4.0-art-dev
